rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /players/{playerId} {
      // Allow any user to read player data (get and list).
      allow read: if true;
      
      // Disallow all write operations (create, update, delete).
      allow write: if false;
    }
  }
}
