
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /players/{document=**} {
      // Allow anyone to read and write to the entire players collection and all subcollections.
      allow read, write;
    }
  }
}
