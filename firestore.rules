rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /players/{playerId} {
      // Allow reading a single player document.
      allow get: if true;

      // Allow listing all player documents.
      allow list: if true;
      
      // Disallow all write operations (create, update, delete).
      allow write: if false;
    }
  }
}
