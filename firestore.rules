
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /players/{playerId} {
      // Allow anyone to read, write, create, update, and delete any player document.
      allow read, write;
    }
  }
}
